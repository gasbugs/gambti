{
  "name": "gambti",
  "version": "0.1.0",
  "description": "Client-side MBTI questionnaire with mini game",
  "type": "module",
  "scripts": {
    "watch": "npx codex watch -p 1227 --src . --out dist",
    "build": "npx codex build --src . --out dist",
    "test": "node -e \"console.log('Tests removed for current workflow.')\"",
    "format": "npx prettier@latest --check \"{scripts,styles,data}/**/*.{js,css,md}\""
  }
}
